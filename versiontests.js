const { version } = require('./package.json');

console.log('WebSocket version:', version);




const WebSocket = require('websocket').w3cwebsocket;
const { version } = require('./package.json');

console.log('WebSocket version:', version);

console.log(WebSocket.version);